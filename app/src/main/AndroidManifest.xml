<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sizdev.arkhirefortalent">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArkhireTalent"
        android:usesCleartextTraffic="true">
        <activity android:name=".homepage.project.detailedproject.DetailOfProjectActivity"/>
        <activity android:name=".homepage.project.waitingproject.ShowWaitingProjectActivity" />
        <activity android:name=".homepage.project.declinedproject.ShowDeclinedProjectActivity" />
        <activity android:name=".homepage.project.approvedproject.ShowApprovedProjectActivity" />
        <activity android:name=".homepage.project.allproject.ShowAllProjectActivity" />
        <activity android:name=".homepage.webviewer.ArkhireWebViewerActivity" />
        <activity android:name=".onboarding.OnBoardingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".administration.ResetPasswordActivity" />
        <activity android:name=".homepage.profile.EditProfileActivity" />
        <activity android:name=".homepage.profile.TalentProfileActivity" />
        <activity android:name=".administration.ForgetPasswordActivity" />
        <activity android:name=".administration.LoginActivity" />
        <activity android:name=".administration.RegisterActivity" />
        <activity android:name=".homepage.HomeActivity" />
    </application>

</manifest>